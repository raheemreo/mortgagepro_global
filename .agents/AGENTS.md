# Rules

- **Git Commits and Pushes**: Never stage, commit, or push files to Git automatically. Always ask for the user's explicit permission before running any `git commit`, `git push`, or Git-modifying commands.
